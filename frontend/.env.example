# API Base URL for production/staging builds
# Leave empty for local development (Vite proxy handles /api routing)
# Staging:    VITE_API_URL=https://uptime-lofi-backend-staging.<account-subdomain>.workers.dev
# Production: VITE_API_URL=https://uptime-lofi-backend.<account-subdomain>.workers.dev
VITE_API_URL=
