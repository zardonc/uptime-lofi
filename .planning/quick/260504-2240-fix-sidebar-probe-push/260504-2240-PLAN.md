---
status: in-progress
created: 2026-05-04
---

# Quick Task 260504-2240: Fix dashboard sidebar navigation and probe push 404

## Tasks

1. Fix sidebar navigation so each sidebar item renders a visibly distinct page instead of silently falling back to Dashboard.
2. Fix generated probe config so `api_url` points to the actual probe push endpoint `/api/push`.
3. Add focused tests and run frontend/backend/probe verification.

## Notes

- `gsd-sdk` is unavailable in this shell, so this quick task is recorded manually under `.planning/quick/`.
- `.planning/STATE.md` is absent in the current workspace, so STATE tracking cannot be updated here.
