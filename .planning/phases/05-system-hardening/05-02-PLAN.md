---
phase: 05-system-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/routes/auth.ts
autonomous: true
requirements: [REQ-005]
user_setup: []

must_haves:
  truths:
    - "Access tokens expire after 60 minutes instead of 15 minutes"
    - "Refresh tokens expire after 30 days instead of 7 days"
    - "Cookie Max-Age matches refresh token TTL (30 days)"
  artifacts:
    - path: "backend/src/routes/auth.ts"
      provides: "Updated TTL constants for access and refresh tokens"
      contains: "60 * 60"
  key_links:
    - from: "backend/src/routes/auth.ts"
      to: "JWT payload exp field"
      via: "sign() call exp parameter"
      pattern: "exp:.*\\+.*60.*60"
    - from: "backend/src/routes/auth.ts"
      to: "Set-Cookie header"
      via: "Max-Age parameter"
      pattern: "Max-Age=.*30.*24.*60.*60"
---

<objective>
Extend Access Token TTL from 15 minutes to 60 minutes and Refresh Token TTL from 7 days to 30 days. This addresses P0-2 from the Cloudflare Free Tier audit.

Purpose: Reduce unnecessary token refresh frequency for legitimate users while maintaining security through stateful refresh token rotation. 15-minute access tokens cause excessive refresh calls; 60 minutes is a better balance for a monitoring dashboard.
Output: Updated TTL constants in auth.ts for login and refresh endpoints.
</objective>

<execution_context>
@C:/Users/inthe/IdeaProjects/uptime-lofi/.opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/inthe/IdeaProjects/uptime-lofi/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/src/routes/auth.ts — Current TTL values:
- Line 122: `expiresAt = Math.floor(Date.now() / 1000) + (7 * 24 * 60 * 60)` — refresh token TTL (7 days)
- Line 133: `exp: Math.floor(Date.now() / 1000) + (15 * 60)` — access token TTL (15 min)
- Line 136: `Max-Age=${7*24*60*60}` — cookie Max-Age (7 days)
- Line 164: `newExpiresAt = Math.floor(Date.now() / 1000) + (7 * 24 * 60 * 60)` — refresh rotation TTL (7 days)
- Line 177: `exp: Math.floor(Date.now() / 1000) + (15 * 60)` — refresh access token TTL (15 min)
- Line 180: `Max-Age=${7*24*60*60}` — refresh rotation cookie Max-Age (7 days)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all TTL constants in auth.ts</name>
  <files>backend/src/routes/auth.ts</files>
  <action>
    1. Define constants at the top of auth.ts (after imports):
       ```typescript
       const ACCESS_TOKEN_TTL_SECONDS = 60 * 60; // 60 minutes
       const REFRESH_TOKEN_TTL_SECONDS = 30 * 24 * 60 * 60; // 30 days
       ```
    2. Replace line 122: `expiresAt = Math.floor(Date.now() / 1000) + REFRESH_TOKEN_TTL_SECONDS`
    3. Replace line 133: `exp: Math.floor(Date.now() / 1000) + ACCESS_TOKEN_TTL_SECONDS`
    4. Replace line 136: `Max-Age=${REFRESH_TOKEN_TTL_SECONDS}`
    5. Replace line 164: `newExpiresAt = Math.floor(Date.now() / 1000) + REFRESH_TOKEN_TTL_SECONDS`
    6. Replace line 177: `exp: Math.floor(Date.now() / 1000) + ACCESS_TOKEN_TTL_SECONDS`
    7. Replace line 180: `Max-Age=${REFRESH_TOKEN_TTL_SECONDS}`
    8. Verify no hardcoded TTL values remain (search for `7 * 24` and `15 * 60`).
  </action>
  <verify>
    <automated>grep -n "7 \* 24\|15 \* 60" backend/src/routes/auth.ts</automated>
  </verify>
  <done>All TTL values use named constants: access=60min, refresh=30days. No hardcoded TTL values remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add cleanup for expired refresh tokens</name>
  <files>backend/src/routes/auth.ts</files>
  <action>
    Add a helper function that deletes expired refresh tokens during login to prevent table bloat:
    ```typescript
    async function cleanupExpiredTokens(db: D1Database) {
      await db.prepare("DELETE FROM refresh_tokens WHERE expires_at < strftime('%s', 'now')").run();
    }
    ```
    Call this function at the start of the /login endpoint (after extracting client IP, before any other queries). This is a lightweight cleanup that runs opportunistically during login — not a replacement for the Cron Trigger (which is Plan 06).
  </action>
  <verify>
    <automated>grep -n "cleanupExpiredTokens" backend/src/routes/auth.ts</automated>
  </verify>
  <done>cleanupExpiredTokens function exists and is called during /login flow</done>
</task>

</tasks>

<verification>
- No hardcoded `7 * 24 * 60 * 60` or `15 * 60` remain in auth.ts
- Named constants ACCESS_TOKEN_TTL_SECONDS and REFRESH_TOKEN_TTL_SECONDS are defined and used
- cleanupExpiredTokens function exists and is called in /login
- wrangler dev starts without errors
</verification>

<success_criteria>
- Access tokens expire after 60 minutes (3600 seconds)
- Refresh tokens expire after 30 days (2,592,000 seconds)
- Cookie Max-Age matches refresh token TTL
- Expired token cleanup runs opportunistically during login
- All TTL values use named constants, no magic numbers
</success_criteria>

<output>
After completion, create `.planning/phases/05-system-hardening/05-02-SUMMARY.md`
</output>
