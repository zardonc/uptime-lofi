# SUMMARY
- Hardened JWT authentication (aud/iss) in dashboardAuth.ts
- Enforced strictRateLimit on /auth/login
- Added input sanitization for nodes routes
- Document JWT_AUDIENCE and JWT_ISSUER in backend/.env.example
